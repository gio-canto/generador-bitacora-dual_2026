name: Prepare Beta 0.36
on:
  push:
    branches:
      - tmp/beta-036
    paths:
      - .github/workflows/tmp-beta-036.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: tmp/beta-036
      - name: Apply Beta 0.36 changes
        run: python .github/patch-beta-036.py
      - name: Commit prepared files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html README.md faq/index.html
          git commit -m "Preparar Beta 0.36 con avisos y justificaciones"
          git push origin HEAD:tmp/beta-036
